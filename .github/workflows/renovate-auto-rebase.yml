name: Renovate Auto Rebase

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions: {}

jobs:
  auto-rebase-renovate:
    name: Auto Rebase Renovate
    uses: TimSchoenle/actions/.github/workflows/maintenance-auto-rebase.yaml@112057eff67d583e17eda2b173bc5d67eb83fb4f # tag=workflows-maintenance-auto-rebase-v1.1.7
    with:
      label: 'dependencies'
    secrets:
      app_id: ${{ secrets.ACTIONS_MAINTENANCE_APP_ID }}
      private_key: ${{ secrets.ACTIONS_MAINTENANCE_PRIVATE_KEY }}
