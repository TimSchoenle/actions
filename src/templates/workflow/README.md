# {{capitalizedPackageName}} {{capitalizedSubAction}}

A reusable workflow for...

## Usage

```yaml
jobs:
  example:
    uses: {{repo}}/.github/workflows/{{subAction}}.yaml@{{tag}}
    with:
      example_input: 'value'
```
