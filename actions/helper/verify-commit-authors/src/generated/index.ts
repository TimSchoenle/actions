/**
 * Entry point of `actions/helper/verify-commit-authors`, bundled to `dist/index.js` and run by GitHub via `runs.main`.
 *
 * AUTO-GENERATED by `bun run generate-action-sources`. Do not edit — regenerate instead.
 *
 * Deliberately free of logic: `src/action.ts` stays importable, and therefore testable,
 * without running the action as an import side effect.
 */
import { run } from '../action.js';

await run();
