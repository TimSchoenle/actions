query VerifyCommits($url: URI!) {
  resource(url: $url) {
    ... on PullRequest {
      commits(last: 100) {
        totalCount
        nodes {
          commit {
            oid
            authors(first: 20) {
              nodes {
                user {
                  databaseId
                }
              }
            }
            signature {
              isValid
              state
            }
          }
        }
      }
    }
  }
}
