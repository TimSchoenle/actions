query VerifyCommits($prUrl: URI!) {
  resource(url: $prUrl) {
    __typename
    ... on PullRequest {
      commits(last: 100) {
        totalCount
        nodes {
          commit {
            oid
            authors(first: 20) {
              totalCount
              nodes {
                user {
                  databaseId
                }
              }
            }
            signature {
              isValid
              state
            }
          }
        }
      }
    }
  }
}
