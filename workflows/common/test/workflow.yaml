name: { { name } }
description: { { description } }

on:
  workflow_call:
    # inputs:
    #   example_input:1
    #     description: 'An example input'
    #     required: true
    #     type: string

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello from reusable workflow test"
